

//- 재사용되는 레이아웃 가져오기 (ex -> react 라면 import 컴포넌트 from "..")
extends layouts/main-layout.pug

block title 
  title #{pageTitle}

block styles
  link(rel="stylesheet", href="/css/main.css")

block pageContent
  h1 #{content}